import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

export const TrackingScreen = () => {
    return (
        <SafeAreaView>
            <View>
                <Text>Tracking booking Screen</Text>
            </View>
        </SafeAreaView>
    );
}