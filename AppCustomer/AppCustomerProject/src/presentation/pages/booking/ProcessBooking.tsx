import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

export const ProcessBookingScreen = () => {
    return (
        <SafeAreaView>
            <View>
                <Text>Process booking Screen</Text>
            </View>
        </SafeAreaView>
    );
}