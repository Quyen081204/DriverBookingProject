import { View, Text } from "react-native"
import { SafeAreaView } from "react-native-safe-area-context";

export const InitBookingScreen = () => {
    return (
        <SafeAreaView>
            <View>
                <Text>Init booking Screen</Text>
            </View>
        </SafeAreaView>
    );
}